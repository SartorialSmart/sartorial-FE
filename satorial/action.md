Actionable steps to resolve backend connectivity and update frontend logos

1. Verify backend is running
   - From project root `sartorial-BE`, activate virtualenv and run:
     - `python manage.py runserver 0.0.0.0:8000`
   - Confirm `http://127.0.0.1:8000/swagger/` returns 200.

2. Confirm API endpoints and auth token shape
   - Check `core/urls.py` for API paths (already under `/api/v1/`).
   - Confirm auth token response fields from backend (SimpleJWT typically returns `access` and `refresh`).
   - If frontend expects `access_token`/`refresh_token`, update frontend or backend to match.

3. Update frontend to use image logo
   - Replace textual logos used as brand with `src/assets/images/Logo.png`.
   - Files to update: `src/pages/home/Home.jsx`, `src/pages/Auth/Login.jsx`, `src/pages/Auth/OrganizationRegister.jsx`.

4. Start frontend
   - From `satorial` folder run:
     - `npm install` (if not already) and `npm run dev`
   - Open `http://localhost:5173/` and verify network requests in DevTools.

5. Reproduce failing request and inspect backend logs
   - With frontend open, trigger the failing flow (login/register) and watch backend console/logs for request path and response.
   - Use `curl -i` to reproduce specific calls if needed.

6. Fix token mismatch (if present)
   - Option A: Change backend JWT response keys to `access_token`/`refresh_token`.
   - Option B: Update frontend Auth handling to read `access`/`refresh` from login response.

7. Verify CORS and allowed origins
   - Confirm `CORS_ALLOWED_ORIGINS` includes `http://localhost:5173` in backend settings.

8. Follow-up
   - Run the full login/register flow and confirm authenticated endpoints return 200 with correct Authorization header.

Notes
- This file was autogenerated by a script-agent. Execute steps locally in the development environment.
